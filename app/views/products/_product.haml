%section.product{id: "#{product.id}"}
  %h2.product_name= link_to product.name, product_path(product), title: product.name
  %p.product_price= product.decorate.price
  %p.product_description= product.decorate.description
